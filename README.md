Data Pipeline Reproducibility (DVC)

This project utilizes [Data Version Control (DVC)](https://dvc.org/) to track and version large datasets, ensuring strict reproducibility without overloading the Git repository.

 Prerequisites
Ensure the DVC library is installed in your Python environment:
```bash
pip install dvc